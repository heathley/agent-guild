# Cloudflare Pages talks to the separately deployed fixed-target Worker.
# Leave empty for the local Vite proxy.
VITE_EDGE_ORIGIN=
# The audited beta package is public and the UI may show its pinned setup command.
VITE_CONNECTOR_PUBLISHED=false
